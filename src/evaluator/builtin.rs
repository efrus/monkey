use crate::object::Object;
use std::collections::HashMap;

pub fn builtins() -> HashMap<String, Object> {
    /*let map = HashMap::new();
    let len = fn(obj: Object) -> Object {
        Object::Null
    }
    map.insert("len", )*/
    todo!("finish this!");
}
